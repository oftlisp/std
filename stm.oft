(module std/stm
  stm
  stm.read
  stm.write)

(defclass stm
  (val value)

  (ctor (initial-value)
    [initial-value])

  (defn read ()
    value)
  (defn write (value)
    (todo "actually implement stm")))
