;; XXX: This is NOT actually the implementation of the std/internal/primitives
;; module.
;; 
;; This is a placeholder module that should trigger an error if your OftLisp
;; implementation does not properly implement the `std/internal/primitives`
;; module.
;; 
;; That module should contain native functions in the runtime; if it does not,
;; this file will be loaded instead, which should trigger errors.
