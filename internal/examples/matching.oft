(module std/internal/examples/matching
  main)

(def counter (atomic-word 0))

(defn test (in)
  (def value (match in
    x x))
  (assert-eq value (atomic-word.inc counter)))

(defn main (args)
  (test 0)
  (test 1)
  (test 2)
  (test 3))
