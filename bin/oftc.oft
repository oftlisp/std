(module main
  [main])

(import std/args
  [make-arg-parser parse-args usage])

(defn main (args)
  (def arg-parser (make-arg-parser oftc (package-path binary-name)
    (option output -o --output)))

  (println (usage arg-parser))

  (def args (parse-args args arg-parser))
  (todo args))
